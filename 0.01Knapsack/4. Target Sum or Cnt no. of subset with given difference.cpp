Count number of subsets with a given difference OR Target Sum

let S1 & S2 be the two subsets
so, S1 - S2 = difference
and S1 + S2 = Sum of array
so S1 = (Sum + Diff) / 2
so find out the number of subsets with given Sum S1

(cnt of subsets with given sum)